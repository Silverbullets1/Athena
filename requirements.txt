# Athena runtime dependencies (core)
# Install with: python3 -m pip install -r requirements.txt
#
# Core: hermes-agent only. Runs the CLI on any platform — Linux,
# macOS, Raspberry Pi (aarch64), WSL2, headless servers.
# No GUI, no PyInstaller.

# Hermes agent harness (required)
hermes-agent>=0.7,<1.0